apiVersion: v1
kind: Service
metadata:
  name: project9-service
spec:
  type: NodePort
  selector:
    app: project9
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007
